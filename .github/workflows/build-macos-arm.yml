name: Build macOS-ARM

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v3

      - name: "Install Homebrew"
        run: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

      - name: "Install required packages."
        run: |
          brew update
          brew install cmake ninja gcc libomp

      - name: "Set up environment"
        run: |
          # echo "LDFLAGS=-L$(brew --prefix llvm)/lib/c++" >> $GITHUB_ENV
          echo "CC=$(brew --prefix gcc)/bin/gcc-14" >> $GITHUB_ENV
          echo "CXX=$(brew --prefix gcc)/bin/g++-14" >> $GITHUB_ENV
          # echo "AR=$(brew --prefix llvm)/bin/llvm-ar" >> $GITHUB_ENV

      - name: "Build"
        run: |
          rm -rf build && mkdir build && cd build
          cmake -DCMAKE_C_COMPILER=$CC \
                -DCMAKE_CXX_COMPILER=$CXX \
                -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_EXE_LINKER_FLAGS="${CMAKE_EXE_LINKER_FLAGS} -Wl,-ld_classic" \
                -GNinja .. 
          ninja
